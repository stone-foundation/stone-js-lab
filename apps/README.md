# Lab apps

Each subdirectory here is a self-contained, runnable Stone.js project that validates a slice
of the framework. Apps are added one at a time and each is confirmed by actually running it.

As an app is added, register it in the root `package.json` `stone.starters` array so it also
becomes available via `stone init --starters github:stone-foundation/stone-js-lab`.
